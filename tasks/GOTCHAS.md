# Gotchas & learnings

Shared brain for build agents. **Read this fully before starting any story.**
Append (never delete) a concise entry whenever you hit something non-obvious: a build
quirk, an API surprise, a decision with a reason. Keep entries to 1–3 lines each.
Format: `- [US-xxx] the gotcha, and what to do about it.`

## Seeded from research (before any code)

- [general] pi packages live under the `@earendil-works` npm scope (recently moved from `@mariozechner`). Pin exact versions (`save-exact`). Docs: https://github.com/badlogic/pi-mono — read `packages/ai/README.md` and `packages/agent/README.md` before using; APIs may differ from blog posts.
- [general] Do NOT read or copy code from github.com/Nadreau/goldfish (it references a provisional patent). Implement independently from our PRD only.
- [general] Toolchain on this machine: Bun 1.2.4 (`/opt/homebrew/bin/bun`), Swift 6.0.3, no Xcode project — we use SwiftPM. Node is available but the brain targets Bun.
- [US-001] SwiftPM does not produce .app bundles. `swift build` yields a bare executable; `scripts/build.sh` must assemble `Minne.app/Contents/{MacOS,Resources,Info.plist}` manually (LSUIElement=true in Info.plist). Dev runs can use the bare executable.
- [US-002] The brain's stdout is reserved exclusively for protocol JSON-lines. Any stray `console.log` corrupts the protocol — route ALL logging to stderr from day one.
- [US-002] pi's own `pi-server`/`pi-protocol` (CBOR) packages are experimental — do not use them; we own a thin JSON-lines protocol defined in `brain/src/protocol.ts`.
- [general] Every story must leave the repo green: `swift build` in `app/`, `bun run typecheck` + `bun test` in `brain/`. Never commit red.
- [general] Commit style: one commit per story, message `US-xxx: <summary>`, ending with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Do not push — the orchestrator pushes after verification.
- [general] Only touch `tasks/GOTCHAS.md` (append) among the tasks/ files — `progress.md` and the PRD are orchestrator/human-owned.

## Learnings (append below)

- [US-001] `bun add --exact -d typescript` now resolves to TypeScript 7.x (the Go-based compiler); `tsc --noEmit` works fine with it under strict settings. Pinned: typescript 7.0.2, @types/bun 1.3.14.
- [US-001] Swift 6 menu-bar skeleton: `@main @MainActor final class ... : NSObject, NSApplicationDelegate` with a custom `static func main()` that sets `NSApp.setActivationPolicy(.accessory)` (Dock-less for the bare dev executable; the bundle gets LSUIElement from Info.plist).
- [US-001] Locate SwiftPM's output with `swift build -c release --show-bin-path` instead of hardcoding `.build/release` — the path is arch-specific (`.build/arm64-apple-macosx/release`).
- [US-001] `bun build --compile src/main.ts --outfile <path>` produces the single-file brain binary (~57MB, embeds the Bun runtime); it runs standalone with no bun install on the target.
- [US-001] `bun build --compile` leaves stray temp files named `.<hash>-00000000.bun-build` in the cwd; add `*.bun-build` to .gitignore or they end up committed.
- [US-001] Minimal working Info.plist keys for the manual bundle: CFBundleExecutable, CFBundleIdentifier, CFBundleName, CFBundlePackageType=APPL, CFBundleShortVersionString, CFBundleVersion, LSMinimumSystemVersion, LSUIElement, NSPrincipalClass=NSApplication. Validate with `plutil -lint`.
- [US-002] Bun 1.2.4 buffers a *piped* stdin until EOF for `Bun.stdin.stream()`, `for await (of console)`, AND node-style `process.stdin` events — deadlocks any request/response protocol. Read fd 0 with node:fs `read()` instead (`stdinChunks()` in brain/src/jsonlines.ts); regression test "responds while stdin is still open" in main.test.ts.
- [US-002] Swift: never iterate `FileHandle.bytes.lines` from an actor-isolated Task — AsyncBytes runs its blocking read(2) inline on the iterating executor, parking the whole actor (deadlocked under `swift test`, only won the race in the app). Bridge readabilityHandler → AsyncStream instead (see BrainClient.lineStream).
- [US-002] `swift test` CAN test an executable target on macOS: `.testTarget(dependencies: ["Minne"])` + `@testable import Minne` works fine despite `@main`. But `swift test` buffers ALL output until the run ends — a hung async test shows nothing; diagnose with `sample <xctest-pid>`.
- [US-002] `bun run script.ts` spawns a *child* bun process; Swift's `Process` holds the parent. When kill-testing crash restart, kill the parent pid (the one Process reports), and beware orphaned children from killed test runs lingering and matching pgrep patterns.
- [US-002] Swift 6 strict concurrency: a mutable local captured by `readabilityHandler` errors ("captured var in concurrently-executing code"); `nonisolated(unsafe) var` is the fix when the closure is serialized on one dispatch source, and `nonisolated(unsafe)` on FileHandle locals is unnecessary (FileHandle is Sendable in the macOS 15 SDK).
- [US-003] pi-ai 0.84.2 real API: `models.login(providerId, "oauth" | "api_key", interaction)` where interaction = `{signal?, prompt(p), notify(e)}`; prompt types text/secret/select/manual_code, notify types info/auth_url/device_code/progress. `models.checkAuth(id)` gives auth state WITHOUT refreshing OAuth (use for status); `models.getAuth(id)` refreshes. Both anthropic OAuth *and* api_key login flows exist on the same provider — api_key login just prompts `secret` via the same interaction.
- [US-003] pi OAuth impls load via bundler-opaque dynamic imports that `bun build --compile` cannot follow — but pi ships the fix: call `registerBunOAuthFlows()` from `@earendil-works/pi-ai/bun-oauth` at startup to register them statically. With that, the real Anthropic OAuth flow (claude.ai authorize URL + localhost callback server) runs fine from the compiled binary. The `.lazy` API wrappers (literal `import()` specifiers) bundle fine without help.
- [US-003] pi CredentialStore contract subtlety: `modify(id, fn)` is the ONLY write path and `fn` returning undefined means "leave unchanged" (not delete — that's `delete()`); Models runs OAuth refresh inside `modify`, so the store must serialize operations (promise chain) or tokens can double-refresh.
- [US-003] `models.login()` can park forever on `interaction.prompt()`; the brain's request loop must dispatch handlers WITHOUT awaiting them (the answering `auth_reply` arrives on the same stdin), and at stdin-EOF pending logins must be aborted or `Promise.allSettled(inFlight)` hangs the exit.
- [US-003] Anthropic's OAuth flow races a localhost callback server against the `manual_code` prompt; when the callback wins, the prompt is cancelled via `AuthPrompt.signal`. An app showing a modal for the prompt should expect it to become stale (the brain answers stale `auth_reply`s with invalid_request — harmless).
- [US-003] Bun.spawn typing under strict TS: don't pass explicit generic params or annotate the reader as DOM `ReadableStreamDefaultReader` (Bun's reader wants `readMany` and a read() arg) — wrap Bun.spawn in a helper and use `ReturnType<typeof helper>`, or a small structural type for the reader.
- [US-003] `bun test` sets no env isolation: subprocess tests touching auth/config MUST set `MINNE_APP_SUPPORT_DIR` (and use `MINNE_MOCK_PROVIDER=1` for the scripted OAuth provider in src/mock-provider.ts) or they'd hit the real `~/Library/Application Support/Minne` and inherit the user's ANTHROPIC_API_KEY.
- [US-004] pi-agent-core 0.84.2 matches its README: `new Agent({initialState:{systemPrompt, model}, streamFn: models.streamSimple.bind(models)})`, `subscribe()` for `message_update`/`text_delta`, `await agent.prompt(text)`, `agent.abort()`. Pin the same version as pi-ai (0.84.2) — they share types.
- [US-004] StreamFn contract surprise: provider failures do NOT reject `agent.prompt()`. They arrive as a final assistant message with stopReason "error"/"aborted" + `errorMessage`, and the Agent PUSHES that failed message into `state.messages`. Inspect the last message after prompt() resolves, and roll errored exchanges out of the transcript yourself or the next turn replays them to the LLM.
- [US-004] Deterministic no-network streaming for tests: `createProvider` accepts a hand-written `api: ProviderStreams` (`{stream, streamSimple}` returning `createAssistantMessageEventStream()`); pushing a terminal `done`/`error` event completes the stream — no `end()` call needed. See mock-provider.ts.
- [US-004] `agent.abort()` reaches the stream function as `options.signal`; a custom stream must watch it and emit `{type:"error", reason:"aborted"}` with the partial message. Protocol decision: an aborted *chat* terminates with `done {aborted:true}` (partial text is valid content); an aborted *login* keeps error code "aborted".
- [US-004] Both main.test.ts and BrainClientTests used `chat` as the canonical "unimplemented" request — without env isolation. The moment chat went live those tests would have made real LLM calls had credentials existed. When bringing a stub to life, grep ALL tests (both languages) for the request type.
- [US-004] Shared subprocess harness now lives in brain/src/test-support.ts (BrainSession, hello); bun test only collects `*.test.ts`/`*.spec.ts`, so helper modules under src/ are safe to import from tests.
- [US-005] `peekaboo menubar click` opens a status-item menu but it closes the moment the peekaboo process exits — you can never screenshot it from a follow-up command. Working recipe: background `osascript` doing `click menu bar item 1 of menu bar 1 of process "Minne"` (menu bar 1, not 2 — LSUIElement apps have no regular menu bar), `sleep`, `screencapture -x`, then key code 53 to dismiss.
- [US-005] SMAppService.mainApp only works from a real .app bundle; the bare SwiftPM dev executable has no bundle identity, so gate the Launch at Login menu item on `Bundle.main.bundleURL.pathExtension == "app"` and disable it in dev runs.
- [US-006] `AXIsProcessTrusted()` reports **true** for a binary launched from a terminal that itself has Accessibility (Ghostty/iTerm): TCC attributes the child to the responsible parent process. Always verify permission behaviour with `open -a build/Minne.app`, never by running the bundle's binary from the shell.
- [US-006] Swift 6 rejects `kAXTrustedCheckOptionPrompt` ("reference to var … is not concurrency-safe") because it imports as a mutable global. Use the literal key string `"AXTrustedCheckOptionPrompt"` in the `AXIsProcessTrustedWithOptions` dictionary.
- [US-006] Calling `AXIsProcessTrustedWithOptions(prompt: true)` is what adds Minne to the Accessibility list; the `x-apple.systempreferences:…Privacy_Accessibility` deep link alone opens a pane with no Minne row to flip. Do both, prompt first.
- [US-006] There is no notification for a TCC grant — poll `AXIsProcessTrusted()` (1s while onboarding is visible, 5s in the background) and also poll on `applicationDidBecomeActive`, which is when the user returns from System Settings.
- [US-006] TCC cannot be revoked programmatically, so the degraded no-capture path is unreachable on a granted Mac. `defaults write sh.minne.app simulateNoAccessibility -bool YES` forces `CapturePermissionState.missing` (fails closed); flipping it back mid-run is also how the live auto-advance was verified.
- [US-006] `swift format` defaults to 2-space indentation and reflows unrelated lines — the repo is 4-space/100-col. Run it with `--configuration` `{"version":1,"lineLength":100,"indentation":{"spaces":4}}` and check `git diff --stat` for files you never touched.
- [US-006] `peekaboo click "<label>" --app Minne` intermittently fails to find AppKit buttons ("increase --wait-for timeout"); `osascript -e 'tell application "System Events" to tell process "Minne" to click button "X" of window 1'` is reliable for driving windows, as is `click menu item N of menu 1 of menu bar item 1 of menu bar 1` for the status menu.
- [US-007] `AXObserverCallback` is a C function pointer, so it cannot capture: pass the instance as the refcon (`Unmanaged.passUnretained(self).toOpaque()`), add `AXObserverGetRunLoopSource` to `CFRunLoopGetMain()` with `.commonModes`, and re-enter with `MainActor.assumeIsolated` — the callback really does arrive on the main thread.
- [US-007] `kAXFocusedWindowChangedNotification` is posted on the *application* element but `kAXTitleChangedNotification` is posted on the *window* element. Register the title notification on the focused window and re-register it every time focus moves, or window-title tracking silently never fires.
- [US-007] AX attribute reads are synchronous IPC into the target app — a hung app would freeze our main thread. Call `AXUIElementSetMessagingTimeout(appElement, 2)` on every app element you create.
- [US-007] AX trees repeat text relentlessly (a container's `AXTitle` is usually its child's `AXValue`); without a `Set` of already-collected strings per snapshot, a small window yields tens of KB of duplicated noise.
- [US-007] Browsers and Finder expose the address as `AXURL` — a CFURL, not a String, and it lives on the web-area/content element deep in the tree, not on the window. Take the first one the walk finds (verified live: Chrome gives `https://…`, Finder gives `file:///…`).
- [US-007] `NSWorkspace.frontmostApplication` will happily hand you Minne itself; skip your own pid or the chat window gets captured into memory and echoes back.
- [US-007] Keep `swift test` AX-free: the tree walk sits behind the `FocusedWindowSource` protocol (only `AccessibilityWindowSource.swift` touches AX) and every scheduling/dedup test drives a scripted fake.
- [US-007] Don't mix a synthetic test clock with `CaptureEngine.start()`, which ticks at the real `Date()`: the per-window debounce then compares a 1970-epoch timestamp against now and skips everything. A test uses either `tick(now:)` on a synthetic timeline or `start()` on the real clock, never both.
- [US-007] Near-duplicate metric is Jaccard over word 3-shingles (`TextSimilarity`), not a token set — a bag of words calls any two windows of the same app identical because they share the UI chrome vocabulary.
- [US-007] A long osascript-driven smoke run dies when the screen locks: `tell application "X" to activate` blocks forever at the login window (and capture starts logging `com.apple.loginwindow`). Keep drive scripts under ~2 min and check `ioreg -n Root -d1 -a | grep CGSSessionScreenIsLocked` — the key is absent when unlocked.
- [US-007] Measured budget: the dev binary averaged 0.1% CPU / 32 MB RSS over 2 min of app switching including 23 KB Finder walks (`ps -o %cpu=,etime=,rss= -p <pid>` gives the lifetime average, which is exactly the "<1% average" criterion).
- [US-008] A greedy regex with optional separators swallows the *next* word or number ("IBAN … 7654 32 please", "card … 1111 123") and then fails its checksum, silently leaking the secret. Don't trust the first match: re-cut it along its separator groups and validate every contiguous sub-span, longest first (`SensitiveMasker.refineByGroups`).
- [US-008] Luhn alone is not enough for cards — spaces between spreadsheet columns glue unrelated numbers into 13–19 digit runs that pass one time in ten. Also requiring a printed card *layout* (one unbroken run, or 3–5 groups of 4–6 digits) cut false positives on a 47 KB page of numbers from 308 to 35.
- [US-008] Masking a full 50 KB snapshot costs ~30 ms on the main thread (five NSRegularExpression rules) — fine at once per 15 s per window. Mask *before* the byte cap: `▮▮▮` is 9 UTF-8 bytes, so masking can grow the text past a cap applied first.
- [US-008] `AXURL` only surfaces during the tree walk, so the domain blacklist can only reject a snapshot after the walk has happened (bundle-id and private-window-title rules run before it). Blacklisted domains therefore cost one discarded walk — no snapshot is ever produced, which is what the story asks.
- [US-008] Chrome ignores every AppleEvent while a "<terminal> wants access to control Google Chrome" TCC prompt is pending — `osascript` just hangs until its 120 s timeout, and the prompt can be hidden behind other windows. Drive Chrome without automation permission instead: `open -na "Google Chrome" --args --incognito <url>` opens a real incognito window, `open -a "Google Chrome" <url>` opens a tab.
- [US-009] macOS ships SQLite 3.43.2 with FTS5 on both sides (system libsqlite3 and Bun 1.2.4's bundled copy). Swift needs `linkerSettings: [.linkedLibrary("sqlite3")]` on the target plus `import SQLite3`; the setting reaches the test target through its dependency on `Minne`.
- [US-009] A read-only SQLite connection to a WAL database needs a *writable directory*, not just a readable file — it recreates `-shm` when missing. Verified: the compiled brain reads `minne.db` read-only while the Swift app holds it open for writing, and also after an unclean exit that left a stray `-wal`. No fallback to read-write needed.
- [US-009] The `sqlite3` CLI refuses `INSERT INTO snapshots` with "unsafe use of virtual table snapshots_fts" — its trusted_schema is off, so the FTS sync trigger is blocked. Prefix hand-written statements with `PRAGMA trusted_schema=ON;`. The app's own connection is unaffected.
- [US-009] FTS5 clamps a term's inverse document frequency at 1e-6, so on a small index every `bm25()` is ~1e-6 and rounding scores to 3 decimals turns them all into 0. Return the raw value (negated so higher is better) and rely on the ordering, not the magnitude.
- [US-009] FTS5 `MATCH` takes an expression, not a search box: a bare `-`, a stray quote, or the word `AND` is a syntax error. `toMatchExpression` in brain/src/sources.ts quotes every term (`"oslo" "trip"`, implicit AND) and keeps only a trailing `*`, so no user input can be a syntax error.
- [US-009] Capture works and persists with the screen locked — the frontmost app is `com.apple.loginwindow` and yields ~11 chars — which makes it a usable headless end-to-end check: run the dev binary with `MINNE_MEMORY_ROOT`/`MINNE_APP_SUPPORT_DIR` pointed at a scratch dir and `-onboardingSeen YES` to suppress first-run.
- [US-009] Both memory roots are env-overridable: `MINNE_MEMORY_ROOT` (~/Minne) and `MINNE_APP_SUPPORT_DIR` (shared with the brain, which resolves `minne.db` the same way). Anything touching disk in the app must go through `MemoryPaths` or tests will write into the real memory.
- [US-009] Source files are append-only, so the *file on disk* owns the snapshot numbering, not an in-memory counter — the app is relaunched mid-hour all the time. `SourceStore` recovers it as `max("## Snapshot N") + 1`; using the max rather than a count means captured text that itself contains such a heading can only skip a number, never collide with an existing citation.
- [US-010] `brain/templates/*.md` is the single source of truth for the wiki schema. Bun imports them as text (`import x from "../templates/SCHEMA.md" with { type: "text" }`) and `bun build --compile` embeds them — verified running the compiled binary from `/` with no files alongside. tsc needs `declare module "*.md"` (brain/src/templates.d.ts) or it can't resolve a `.md` import.
- [US-010] Seeding stayed in Swift (MemorySeed) rather than moving to the brain: first run must seed with no brain running, and SourceStore.init already does it. Swift keeps a *copy* of the three bootstrap templates and `MemorySeedTests.testTemplatesMatchTheBrainsCopy` reads `brain/templates/` via `#filePath` and fails on any drift. Never edit the Swift literals directly — edit the templates and paste.
- [US-010] The Swift literals are a straight paste (the templates contain no `\` or `"""`): 8-space indentation and a blank line before the closing `"""`, which is what produces the trailing newline the files have.
- [US-010] Frontmatter is parsed by a hand-written strict subset (brain/src/frontmatter.ts), no YAML dependency: flat `key: value`, scalars and lists only. Following YAML, `sources:` with nothing after it parses as **null**, not `[]` — an empty list must be written `[]`, and wiki-lint enforces that.
- [US-010] wiki-lint severity rule: errors are malformed or dangling (bad frontmatter, missing required field, unresolvable `[[link]]`, citation that isn't `sources/DATE/HHmm-app.md#N`); warnings are drift (orphan, page with no citations, cited source no longer on disk). Retention deletes sources the wiki still cites, so `citation_missing` can never be an error.
- [US-010] Source slugs come from the app's own name and can hold any Unicode letter/digit (SourceDocument.slugify keeps `CharacterSet.alphanumerics`), so the citation pattern uses `[\p{L}\p{N}-]+`, not `[a-z0-9-]+`.
- [US-010] `bun test` on the whole brain suite flaked once on "search_sources over the protocol > returns ranked snippets" (subprocess start-up timing); it passes in isolation and on every rerun. Re-run before believing a red suite that names only a subprocess test.
- [US-011] pi tool API (pi-agent-core 0.84.2): `AgentTool` = pi-ai's `Tool` (`name`/`description`/typebox `parameters`) plus `label` and `execute(toolCallId, params, signal?, onUpdate?)`. The loop validates arguments against the schema before calling execute (`validateToolArguments`), so a tool never sees wrong types; a failing tool must *throw* (the loop turns it into `isError: true`), never return the error as content. Tools reach the agent via `new Agent({initialState:{tools}})` or `agent.state.tools`.
- [US-011] typebox is pi's schema library and was only a transitive dependency — pin the exact version pi-ai uses (1.3.7) before importing `Type` yourself, or `TSchema` is a structurally different type. `bun build --compile` bundles it with no help; verified by running a write_page + search_memory round trip from the compiled binary launched from `/`.
- [US-011] Path safety needs both halves: reject `..`/absolute/NUL lexically AND `realpath` the deepest *existing* ancestor before comparing against a realpath'd root. Without the second, a symlink planted under `wiki/` escapes; without resolving the root too, macOS's `/tmp` → `/private/tmp` makes every legitimate write in a test look like an escape.
- [US-011] `write_page`/`append_log` lint the candidate tree and refuse only the issues the write *introduces* (diff against a lint of the tree as it was, keyed by code+path+detail — not line, which shifts when index.md grows a row). Refusing on the whole report would let one page the user broke in Obsidian block every future write.
- [US-011] Severity, as chosen for write_page: a dangling `[[link]]` is a wiki-lint error, so the write is refused — create the page you link to first (SCHEMA.md asks for exactly that). `no_sources` and `citation_missing` are warnings and come back with the result. `last_updated` is stamped from the clock and is not an argument the model can pass.
- [US-011] The wiki has no search index and does not need one: `search_memory` scans `wiki/**.md` and scores weighted whole-word term counts (title 4, summary 3, path 2, body 1, ANDed like FTS5). index.md, log.md and SCHEMA.md are deliberately excluded — index.md lists every page and would otherwise match nearly every query.
- [US-011] `MINNE_MEMORY_ROOT` is now set by the shared subprocess harness (test-support.ts) alongside `MINNE_APP_SUPPORT_DIR`. The memory tools *write*, so any protocol test that forgot it would edit the user's real `~/Minne`.
- [US-011] mock-provider understands `TOOL: <name> <json args>` (and answers a returned toolResult with prose), which is how a tool round trip is exercised through the real agent loop, the real tools and the real protocol without a network — see chat.test.ts "a chat turn can write memory and read it back through its tools".
- [US-012] The sync watermark is the app's `snapshots.id` rowid, persisted in `sync-state.json` in the app-support dir (brain-owned, atomic tmp+rename) — NOT in minne.db, whose schema the Swift app owns and the brain only ever opens read-only. Retention prunes the oldest rows, so the mark stays meaningful; `max(id) < watermark` can only mean the index was wiped, and resets it to 0.
- [US-012] Idempotency is checked before authentication: a pass counts the backlog first and returns `idle` if nothing is past the watermark, so a scheduled tick costs one `count(*)` and never constructs an Agent. The test asserts it via `mockStreamCalls()` in mock-provider.ts, not via what landed on disk.
- [US-012] Bringing `ingest` to life again caught the trap from US-004: `brain/src/main.test.ts` and `app/Tests/MinneTests/BrainClientTests.swift` both spawned the brain with an *inherited* environment, so an ingest there would have digested the user's real captures with their real credentials into their real `~/Minne`. Both now set MINNE_APP_SUPPORT_DIR/MINNE_MEMORY_ROOT to scratch dirs (Swift does it with `setenv` in `override class func setUp()`, since the child inherits the test process's env).
- [US-012] mock-provider now runs *multi-step* scripts: every `TOOL: <name> <json>` line in the last user message is emitted one per turn (progress counted from tool results since that message), and MINNE_MOCK_SCRIPT supplies the same lines for prompts a test does not compose (the ingest and lint passes build their own). `FAIL:` matches any line for the same reason.
- [US-012] `seedSnapshotIndex` now ends with `PRAGMA wal_checkpoint(TRUNCATE)`: seeded rows otherwise sit in `-wal` and a brain subprocess opening the file read-only in that window sees an empty index — the intermittent "no results" flake noted under US-010.
- [US-012] Chat and the sync pass are independent Agents that both write log.md and the wiki; no locking. `write_page`/`append_log` are synchronous per call and last-write-wins is fine at one pass per 30 min. What *is* serialized is passes against each other: a second `ingest` while one runs returns `busy` rather than queueing.
- [US-012] Cost containment lives in three env-overridable numbers (settingsFromEnv in ingest.ts): MINNE_SYNC_BATCH_SIZE (12 snapshots per model turn), MINNE_SYNC_MAX_BATCHES (4 turns per pass, the rest waits for the next tick), MINNE_SYNC_MAX_TURNS (12 tool round trips per batch, enforced with the Agent's `shouldStopAfterTurn`). MINNE_SYNC_INTERVAL_MS / MINNE_LINT_INTERVAL_MS retune or (at 0) disable the timers; test-support sets both to 0.
- [US-012] The scheduler's `setInterval` handles get `unref()` and are cleared in `shutdown()` — verified that the compiled binary still exits 0 at stdin EOF with the 30 min timer armed.
- [US-012] The lint pass bootstraps the three root files before linting: a memory missing index.md lints as an error the agent has no tool to repair (write_page refuses anything outside `wiki/`). An orphan, by contrast, is fixed by simply calling write_page on the page — that rewrites its index.md entry and makes it reachable again.
- [US-013] `BrainClient` settles `done`/`error` on the *request* continuation, so a chat's terminal outcome never appears on `client.events`. The chat view model therefore takes deltas/tool_calls from the event stream and the outcome from the request's completion — that split is what `ChatBackend` exists to express.
- [US-013] No new protocol request was needed for "New chat": `chat.newChat: true` already resets the brain's session (US-004). But the brain only resets once a turn reaches the agent, so a turn that failed (`not_authenticated`, transport) leaves the reset owing — restore the flag or the retry silently continues the old session.
- [US-013] Carbon `RegisterEventHotKey` (⌥Space = `kVK_Space` + `optionKey`) needs no Accessibility grant, unlike `NSEvent.addGlobalMonitorForEvents`. Its `EventHandlerUPP` is a C function pointer and cannot capture: pass the closure literal inline and look the action up by `EventHotKeyID.id` in a main-actor dictionary.
- [US-013] `AttributedString(markdown:)` interprets inline styling but collapses block structure (lists, headings, fences all become one paragraph). Split blocks yourself (`ChatMarkdown.blocks`, pure and unit-tested) and parse each with `.inlineOnlyPreservingWhitespace`.
- [US-013] `NSPanel` + `override var canBecomeKey { true }` lets an `.accessory` app focus a text field. `setFrameAutosaveName` only starts *recording* the frame — call `setFrameUsingName` first (and `center()` when it returns false) or the window opens centred every launch. Verified: 620×700 at (300,200) survives a relaunch.
- [US-013] `screencapture -x -l <windowid>` composites windows that were never ordered in — a good-looking capture is NOT evidence the window is on screen. Confirm with a region capture (`-R x,y,w,h`) before believing a UI state.
- [US-013] Never send blind keystrokes when driving an accessory app: `first process whose frontmost is true` reports the *previous* app (Chrome/Ghostty) even while Minne's panel is key, and `set frontmost to true` on process "Minne" does not focus the panel. A stray `keystroke` typed a prompt into another app's window. Working recipe: `peekaboo click --coords <inside the field>` (activates the app and focuses it), confirm the caret with a region capture, then `peekaboo type "…" --return`. peekaboo has no key-name flag — `{escape}` is typed literally; send real Escape with `osascript … key code 53`.
- [US-013] Minne's SwiftUI panel exposes no AX children to System Events (`entire contents of window 1` is empty), so scripted UI driving has to be coordinate-based, not element-based.
- [US-013] Dev run against the mock provider needs no login flow: seed the app-support dir with `config.json` `{"provider":"mock"}` and `auth.json` `{"mock":{"type":"oauth","access":"mock-access","refresh":"mock-refresh","expires":<future ms>}}`, then launch with `MINNE_MOCK_PROVIDER=1` and scratch `MINNE_APP_SUPPORT_DIR`/`MINNE_MEMORY_ROOT`.
- [US-013] mock-provider gained two UI-verification knobs: `MINNE_MOCK_SLOW_DELTA_MS` (stretch the "SLOW" stream so a mid-stream screenshot is not a race) and `MINNE_MOCK_REPLY` (stream one exact answer, e.g. markdown, once any `TOOL:` script is spent).
- [US-013] The brain has no prettier config — `bunx prettier --write` reformats a whole file to 80 columns and buries the real diff. Match the existing style by hand.
- [US-013] Minne owns an *untitled 500×500 window at the screen's bottom-left* that never renders: it is a SwiftUI hosting artifact (both `NSHostingView` and `NSHostingController` produce it; a trivial `Text` view does not, the composer's multiline TextField/`@FocusState` does). It has no backing store, so `screencapture -l <id>` on it fails with "could not create image from window". The chat panel is the window *titled "Minne"* — target that one, not index 1 of the window list.
- [US-013] `NSWindow.isKeyWindow` stays **true** while the app is inactive and while the window sits on another Space. A hotkey toggle keyed on it alone closes a window the user cannot see (⌥Space then looks dead). The predicate has to be `isVisible && isKeyWindow && isOnActiveSpace && NSApp.isActive`.
- [US-013] `collectionBehavior = [.moveToActiveSpace, .fullScreenAuxiliary]` is what lets a panel be summoned to the user: `.fullScreenAuxiliary` to appear over a fullscreen app's Space at all, `.moveToActiveSpace` to follow. `.moveToActiveSpace` only acts when the window is *ordered in* — one already visible on another Space stays put, so `orderOut(nil)` first when `!isOnActiveSpace`.
- [US-013] Activation is asynchronous across a Space transition: `NSApp.activate` + `makeKeyAndOrderFront` in one go leaves the panel visible but *not* key (drawn inactive, keystrokes still go to the other app). Re-assert both ~0.2 s later, guarded on `!isKeyWindow || !NSApp.isActive`.
- [US-013] Driving another app for these tests: `tell application "Google Chrome" to activate` hangs on the automation TCC prompt (see US-008) — use `open -a "Google Chrome"`. Toggling fullscreen with System Events' `AXFullScreen` attribute works and needs no automation grant, but it does **not** change which app is active, which quietly invalidates any "another app is in front" assumption.
- [US-013] **Streamed deltas are not the record.** `BrainClient` settles `done` straight onto the request continuation while `text_delta`s ride the `events` AsyncStream, so on a fast turn the last deltas arrive *after* the UI has already finished the turn — the transcript keeps a truncated answer ("Yesterday you" and nothing more). `.bufferingNewest(256)` can also drop deltas in a burst. `chat`'s `done` result therefore carries `text`, and the transcript is reconciled against it when the turn settles.
- [US-013] That `text` is the concatenation of the text parts of *every* assistant message the turn produced, not `messages.at(-1)`: a turn spans one assistant message per tool round trip, and prose written before a tool call is part of the answer too (`assistantText` in service.ts).
- [US-013] Every UI test of streaming used the mock's `SLOW` path, which is exactly why this survived verification: 400 ms between deltas gives the event stream all the time in the world to drain. Test a *fast* reply (`MINNE_MOCK_REPLY` without `SLOW`) whenever streaming ordering is in question.
- [US-014] Almost the whole story was already in the brain: US-003 shipped `login`, `logout`, `configure` and a `status` that reports per-provider auth. The only brain change needed was adding `models: [{id,name}]` per provider to `status` so the app can render a model picker without a second request. Check what the protocol already does before adding to it.
- [US-014] pi's keyless local provider (ollama) answers `checkAuth` with `{type:"api_key"}` and no stored credential — it is "configured", never "signed in". A UI that says "Signed in" off `authenticated` alone tells the user nonsense about a server on their own Mac; `AuthModel.statusLine` distinguishes "Ready"/"In use" from "Signed in".
- [US-014] `anthropic` registers both `oauth` and `api_key`, so one provider id backs two onboarding cards. Restoring the UI from `status` needs the credential's `authType` to tell them apart (`ProviderCatalog.choice(forProvider:authType:)`), not the provider id.
- [US-014] `URL(string: "localhost:11434")?.scheme` is **"localhost"** — `URL(string:)` validates nothing useful. A base-URL field has to check the scheme is http/https *and* the host is non-empty (`AuthModel.isServerURL`).
- [US-014] `@Observable` notifies SwiftUI, not AppKit. AppKit renderers need an explicit observer list, and it must hold its owner **weakly** (`AuthModel.observe(_:_:)`) — onboarding windows are recreated on every open, and a strong list would render into dead ones forever.
- [US-014] Unlike the SwiftUI chat panel (US-013), the AppKit onboarding window exposes a full AX tree: `click radio button "Local (Ollama)" of window 1`, `click button "Sign In" of window 1`, and `set value of text field 1 of window 1 to "424242"` all work through System Events — no coordinates, no focus games, no blind keystrokes.
- [US-014] `peekaboo click --coords` inside the onboarding window activated *Chrome* instead of Minne (the window was visible but not frontmost). `osascript … set frontmost to true` + `perform action "AXRaise" of window 1` first, then drive by AX element name.
- [US-014] mock-provider gained `MINNE_MOCK_AUTO_CODE` (+ `MINNE_MOCK_AUTO_CODE_MS`): the manual-code prompt goes up and is then abandoned through its `signal`, exactly as pi's Anthropic flow does when the localhost callback wins. It drives a login to completion with no input — and puts the UI through the stale-prompt case, which is how that path got verified for real.
- [US-014] The app mirrors the brain's `MINNE_MOCK_PROVIDER=1` flag: with it set, `ProviderCatalog.cards` appends a fifth "Mock provider (tests)" card, which is the only way to exercise the whole OAuth flow in the UI without a real account. Debug launch args `-onboardingStep provider` and `-autoSignIn <choice>` reach the provider step and start a sign-in without any clicking.
- [US-015] `logout` with no `provider` already clears **every** stored credential (service.ts lists the store), so "delete all memory" needed no new protocol request — and credentials must go through the brain, which holds them in memory too; deleting `auth.json` under a running brain leaves it signed in.
- [US-015] An `NSScrollView`'s document view is unflipped by default, so pinning content to its top and calling `scroll(to: .zero)` opens every section scrolled to its *end*. Give the document view `isFlipped = true` (`FlippedView` in SettingsWindowController).
- [US-015] With `toolbarStyle = .preference` the titlebar is ~80 pt tall and `setContentSize` does not account for it — every section opened a toolbar's worth too short. Size the *frame* instead: `chrome = window.frame.height - window.contentLayoutRect.height`, then `setFrame(height: wanted + chrome)`.
- [US-015] A section's `fittingSize` can be stale when the model notifies: the account card grows when `status` fills the model picker, and the window controller observes *before* the views that render it do. Measure once now and once more on the next runloop pass (`DispatchQueue.main.async`), or the window keeps the size the section briefly wanted.
- [US-015] `NSToolbar` does not always follow a click through to `selectedItemIdentifier` (an AX-driven click never does), which left the toolbar highlighting a section other than the one on screen. Set it explicitly in the item's action.
- [US-015] The settings window exposes its whole AX tree flat under `scroll area 1` (`text field 1`, `button "Add"`, `pop up button 1`, `sheet 1`), so every control is scriptable: `set value of text field 2 … / click button "Add"`, and `keystroke "delete"` into the confirmation sheet really does enable the destructive button (the sheet is key, unlike the SwiftUI panel of US-013).
- [US-015] Live-effect verification that is actually evidence: blacklist Ghostty in the UI, activate Ghostty, and the running app logs `capture skipped — blacklisted app: Ghostty`; plant a `sources/<30 days ago>/` directory and drop retention to 5 days, and the sweep runs at once (`retention: pruned … 1 day(s)`). Both without a restart.
- [US-015] Settings' persisted keys live in one place (`SettingsStore`): the blacklist is written as the *whole* list rather than the user's additions, so removing a shipped default sticks, and an absent key still means "never edited" (`CaptureBlacklist.standard`). Retention keeps US-009's `retentionDays`.
- [US-015] Debug launch args for the window: `-settingsSection account|privacy|memory|general` opens it at a section and `-settingsSheet delete` puts the typed-confirmation sheet up — the only way to screenshot that state with the screen locked (`screencapture -x -o -l <windowid>`; `peekaboo list windows --app Minne` gives the id, and `--app Minne` alone hits the phantom 500×500 window from US-013).
- [US-016] The hardened runtime kills the compiled Bun brain at startup ("Ran out of executable memory", SIGTRAP). Sign it with `com.apple.security.cs.allow-jit` (`scripts/minne-brain.entitlements`); JSC allocates its JIT through MAP_JIT, so `allow-unsigned-executable-memory` is *not* needed — verified by a full agent turn (login, tool call, wiki write) under `codesign -o runtime`.
- [US-016] The brain moved from `Contents/Resources/minne-brain` to `Contents/MacOS/minne-brain`: a Mach-O under Resources is sealed as a *resource*, which codesign and notarization both reject. `BrainLaunch.locate()` now looks beside `Bundle.main.executableURL`, and nested code must be signed before the enclosing bundle.
- [US-016] `codesign -s -` (ad-hoc) accepts `-o runtime --entitlements` and needs no identity or keychain unlock — it is a complete local rehearsal of the release signing path, and `codesign --verify --strict --deep` then proves the nested brain was sealed as code rather than as a resource.
- [US-016] Version has one source: the repo-root `VERSION` file. `scripts/build.sh` interpolates it into Info.plist and `main.ts` imports it as text (`with { type: "text" }` + `declare module "*/VERSION"` in templates.d.ts) so `bun build --compile` embeds it. The release workflow refuses a tag that disagrees with it.
- [US-016] CI step order matters: `bun install` in `brain/` must precede `swift test`, because BrainClientTests spawns the real brain with `bun run src/main.ts` and that needs `brain/node_modules`.
- [US-016] GitHub Actions: the `secrets` context is NOT available in a step's `if:` — compute the flag into job-level `env` (`HAS_SIGNING`) and test `env.HAS_SIGNING == 'true'`. Signing in CI also needs the .p12 imported into a keychain made default; env vars alone cannot sign.
- [US-016] Workflows can be linted locally without installing anything: drop the `actionlint` and `shellcheck` release binaries into a scratch dir and run `PATH=<scratch> actionlint` (actionlint only checks `run:` blocks when shellcheck is on PATH).
- [US-016] There is no such thing as a usefully "unsigned" .app on arm64: the linker ad-hoc signs the executable, and that lone signature claims a CodeResources seal the assembled bundle never got, so `codesign --verify --strict` fails with "code has no resources but signature indicates they must be present". release.sh therefore always signs — ad-hoc (`-s -`, `--timestamp=none`) when MINNE_SIGN_IDENTITY is unset.
- [US-016] macOS bash is 3.2, where `set -u` treats an *empty* array expansion `"${ARR[@]}"` as an unbound variable. Pass the optional codesign flag as a plain string (`--timestamp` vs `--timestamp=none`) instead of an array that is sometimes empty.
- [US-017] Right-Option is a *bare modifier*, so Carbon `RegisterEventHotKey` (US-013) cannot see it — it needs a `CGEventTap` on `flagsChanged`. `.maskAlternate` is set for both Option keys; the device bit `NX_DEVICERALTKEYMASK` (0x40) is what says *which*, and keycode 0x3D says it is Option at all. Press = bit set, release = bit clear.
- [US-017] The tap must return every `flagsChanged` event **untouched**. Consuming one costs nothing in typed characters (a bare modifier types nothing) but breaks ⌥-click, ⌥-drag and every Option shortcut system-wide, because the app underneath never learns the key went down. It is still a `.defaultTap` rather than `.listenOnly`, for one reason only: swallowing Escape while the overlay is up.
- [US-017] `CGEvent` is a class and not `Sendable`, so it cannot be captured into `MainActor.assumeIsolated` — Swift 6 rejects it. Read the keycode, flags and location in the C callback into a plain struct (`MinneKeyTap.Signal`) and hand *that* across. Bonus: the handler then takes only values and is unit-testable.
- [US-017] The tap callback runs on the main run loop, in line with the user's typing. It must do nothing but bookkeeping and `DispatchQueue.main.async` the real work — one inline AX round trip would stall every keystroke on the machine.
- [US-017] `kCGEventTapDisabledByTimeout` is not theoretical: **verified live**, the capture engine's own AX tree walk (a 16 KB Notes window) blocked the main thread long enough for the system to switch the tap off. Re-enabling in the callback recovers it, but the press in flight is lost. If that becomes annoying, move the tap's run-loop source to a dedicated thread — which then needs the overlay-visible flag as cross-thread state, since Escape must be answered synchronously.
- [US-017] Overlay ordering is the opposite of the chat panel's (US-013): `.nonactivatingPanel` + `canBecomeKey = false` + `orderFrontRegardless()` so the target app **keeps focus and its caret** — `makeKeyAndOrderFront` would take both, and US-018 needs the field it is pointing at. `.canJoinAllSpaces` rather than `.moveToActiveSpace`: the overlay is transient, so it should simply exist wherever the user is.
- [US-017] AX rectangles are top-left-origin on the *primary* display; AppKit's are bottom-left-origin on the same display. The flip constant is `NSScreen.screens[0].frame.height` — not `.main` (that is the screen with the key window, which an accessory app with no key window does not have) and not `visibleFrame` (which would shift everything by the menu bar).
- [US-017] `AXBoundsForRange` over a zero-length selection returns nothing in some apps — retry the same location with length 1. An all-zero rect means "I don't know", so a caret at the exact origin has to be treated as unusable and fall back to the focused element's leading edge. Measured: TextEdit, Notes, Mail, Safari and Chrome give a real caret; Slack and VS Code always take the element fallback.
- [US-017] Password fields: WebKit *and* Chrome expose the input as `AXTextField` whose **subrole** is `AXSecureTextField`, so the subrole check is the one that matters and it has to run before the "does it have AXSelectedTextRange" test (a password field answers that like any other field). Verified live in Safari, Chrome and Mail — no overlay, and a log line saying why.
- [US-017] VS Code is AX-dark by default: `AXFocusedUIElement` on the application element returns **nothing at all**, app-wide, not just in the editor. Launching with `--force-renderer-accessibility` (or setting `editor.accessibilitySupport: "on"`) turns on the tree and it becomes an ordinary `AXTextArea` with `AXSelectedTextRange`, `AXBoundsForRange` and `AXReplaceRangeWithText` — worth knowing for US-018's insertion path.
- [US-017] macOS's own password autofill popover ("Passwords…") steals focus and emits an extra right-Option flags pair, which the discriminator reads as a second tap. Harmless as it stands (nothing is focused by then, so nothing appears), but it is why a stray "no text field is focused" line follows every password-field test.
- [US-017] `peekaboo capture live --mode region --region x,y,w,h --video-out out.mp4` is how a *transient* overlay gets recorded: it writes changed frames plus a `contact.png` contact sheet, which is far more readable as evidence than an mp4. Driving ⌥+key inside the recording is what proves the overlay stayed away while the character still typed through.
- [US-017] Two test traps: `.nan` is ambiguous between `CGFloat` and `Double` in a test file (write `CGFloat.nan`), and never assert an exact time-window boundary — `1.3 - 1.0` is `0.30000000000000004`, so a "released exactly at the window" test fails on the arithmetic, not the logic.
- [US-018] **Chromium applies an AX text write asynchronously.** It answers `.success`, the field still reads as it was, and the text appears a moment later. Verifying with a single read-back therefore called every AX path dead, ran the pasteboard fallback, and inserted the draft **twice** in Brave (verified live). `AccessibilityFieldWriter.landed` polls the value for up to 120 ms instead. Any "did my AX write land?" check needs the same treatment.
- [US-018] Measured insertion paths: TextEdit takes `AXSelectedTextRange` + `AXSelectedText` (surgical, and TextEdit's own ⌘Z still reverses it afterwards); Safari and Brave textareas refuse that and take `AXValue`; the pasteboard fallback works everywhere. Try selected-text → value → paste in that order and *verify each*, because two of the three answer `.success` for writes they ignore.
- [US-018] The pasteboard fallback must restore **every type of every item** (`NSPasteboardItem.types` → data), not the string: a user who copied an image gets it back as its alt text otherwise. Restore on a delay (0.6 s) — the target app reads the pasteboard when it gets round to the synthetic ⌘V, so an immediate restore pastes the user's old clipboard instead of the draft. Verified live in Safari: clipboard contents survived the swap intact.
- [US-018] Mode detection has to be decided from AX state at press time and nothing else (selection → rewrite, non-empty field → instruction, else infer). Note that a whitespace-only field is `infer`, and that makes the caret position meaningful there — the draft goes in *at* the caret so the user's blank lines survive.
- [US-018] Undo is the insertion machinery pointed backwards: keep the `FieldEdit` that went in and apply its `inverse` (same start, the draft's length, the previous text). Ranges are UTF-16 units because that is what AX speaks — one emoji is two units, and a Character count silently cuts it in half.
- [US-018] `draft` streams no `text_delta`s on purpose: the field may not be touched until the draft is whole, so a partial one has nowhere to go. `tool_call` events do go out, which is what the overlay's "Searching your memory…" line renders.
- [US-018] An `NSTextView` inside an `NSScrollView` is laid out by frames, not Auto Layout: handed to a stack view with no frame and no container width it renders **nothing at all** (verified — the overlay showed an empty gap where the draft should be). A wrapping `NSTextField` with `preferredMaxLayoutWidth` is the right control here; but setting `lineBreakMode = .byTruncatingTail` on one turns wrapping off and puts the whole draft on one truncated line, so elide long drafts by trimming the string yourself.
- [US-018] Buttons in a `.nonactivatingPanel` need `acceptsFirstMouse` overridden to true, and they do work while the target app keeps focus — verified live: clicking Insert in Safari inserted the draft and Safari stayed frontmost.
- [US-018] peekaboo's `click --coords` and its `list windows` bounds did not agree with the overlay's real position (clicks 30 pt off landed on the page behind it, dismissing the panel). Log the panel's own `NSWindow.frame` and post a `CGEvent` mouse click at `primaryScreenHeight - frame.maxY` — the event tap's own coordinate space — when a borderless panel's buttons have to be driven.
- [US-018] Live UI verification on a machine in a Zoom call: Chrome grabbed focus roughly every 10 s and dismissed the overlay (correctly — app switch dismisses). Guard every synthetic keystroke on the frontmost app being the one under test and abort otherwise, or a stray ⌘V lands in the user's meeting.
- [US-018] Adding a page type touches five places that must move together: `PAGE_TYPES` + `REQUIRED_FIELDS` + `PAGE_TEMPLATES` + `pagePath` in wiki.ts, `INDEX_SECTIONS` in memory.ts, a `brain/templates/*.md`, the prose in SCHEMA.md, and — because index.md gained a section — the pasted copies in `MemorySeed.swift` (regenerate them from `brain/templates/`, indented 8 spaces with a blank line before the closing `"""`; `MemorySeedTests` fails on any drift).
- [US-018] The mock provider echoes the prompt it was given (`echo: <text> …`) when `MINNE_MOCK_REPLY` is unset — which is how a protocol test asserts on what actually reached the model: `draft.test.ts` proves the style page's own words and its citation path were in the prompt, over the wire, with no network.
- [post-v1] **An AX write into a web editor is erased, not rejected.** A browser composer (LinkedIn, and any React contenteditable) renders from state the framework holds; `AXValue`/`AXSelectedText` change the DOM without an input event, so the state never moves and the next re-render — a blur, a click, a keystroke — paints the old text back. It reads back perfectly in between, so no verification can catch it. Web content therefore takes the pasteboard path only (`InsertionPolicy`); ⌘V is a real input event the framework processes and owns. Reproduced and fixed against a local React-style contenteditable (`scratchpad/webrepro.html`, which mirrors DOM *and* framework state into `document.title` so a driver can read both with `axprobe title`).
- [post-v1] Detecting web content: walk `AXParent` from the focused element for an `AXWebArea` — **1.3 ms for the twelve levels of a Chrome tab**, and precise where a bundle-id list is a guess (it says yes to an Electron editor and no to a browser's own address bar). Chrome focuses a contenteditable as an `AXTextArea` whose parent is the `AXWebArea`. The browser bundle ids stay as a backstop for an app that will not show its ancestors.
- [post-v1] A paste replaces the *selection*, so each mode has to arrive with the right thing selected (`SelectionPlan`): rewrite and infer already do (the user's selection, the caret), instruction needs a synthetic ⌘A first. Never ⌘A an empty field — in a browser that can select the whole page.
- [post-v1] Undo after a paste belongs to the **app**: the paste went through its event pipeline, so it is on its undo stack, and an inverse AX write would be repainted away exactly like the insertion was. So the tap does not consume ⌘Z after a paste (no synthetic-⌘Z loop through our own tap either) and the Undo button posts ⌘Z instead. After an AX write undo stays ours, because setting `AXValue` flattens the app's own undo.
- [post-v1] A paste cannot be verified as it is posted (the keystroke is delivered when the app gets round to it) and must never be retried (a second ⌘V on top of one that landed inserts twice). Check it *asynchronously* ~0.6 s later and only leniently — readable-and-completely-unchanged is the only honest failure, because a rich editor normalises what it took. Blocking the main thread that long would trip the event tap's own timeout (US-017).
- [post-v1] `pkill -f 'build/Minne.app/Contents/MacOS/Minne$'` silently matches nothing once the app is launched with arguments, so test instances pile up — two live event taps, two overlays, and one of them consuming the Return the other was waiting for. Kill with `'MacOS/Minne '` *and* the anchored form, and check `ps` afterwards.
- [post-v1] Replacing the executable inside `build/Minne.app` invalidates the ad-hoc signature the bundle carries and the app is **SIGKILLed at launch** ("Killed: 9", nothing in the log). Re-sign after every swap: brain first with `scripts/minne-brain.entitlements`, then the bundle (`codesign --force -o runtime -s -`). The Accessibility grant survives it.
- [post-v1] A stock `NSButton` in an app that is never active always draws in its *inactive* state: `bezelColor` is ignored, so an accent-tinted primary comes out the same flat grey as the buttons beside it. The overlay's buttons paint their own capsule (`OverlayButton`). Note that assigning `attributedTitle` also rewrites `title`, so a button that rebuilds its title from `self.title` appends its key hint again on every appearance change.
- [post-v1] `NSBox` lays its content view out by frame: a wrapping `NSTextField` inside one measures as zero and the box collapses to nothing (the draft simply did not appear). Use a layer-backed `NSView` with real constraints. And any `CGColor` on a layer is a *resolved* colour — re-apply it in `viewDidChangeEffectiveAppearance` or the panel keeps its light-mode greys in the dark.
- [post-v1] Debug hooks for the overlay, since none of its states can be reached by hand with the screen locked: `-minneKeyPreview working|consulting|result|inserted|undone|failed` puts the panel up with no key press and no model, and `-minneKeyPreviewAppearance dark|light` forces the palette (AppKit takes the appearance from the system and ignores `-AppleInterfaceStyle`). Note the panel that `MinneKeyController` owns is *also* in the window list, unpresented, at the screen edge — filter on x>100 or you screenshot the empty one.
- [site] **Never launch a second Minne while the user's own instance runs.** Two instances mean two live `CGEventTap`s and two overlays, and driving one with peekaboo/osascript steals focus mid-sentence from whoever is typing — it garbled the user's typing during the site build. Screenshots for docs come from the captures already in the session scratchpad, and a page is verified with the headless Playwright MCP tools, which render offscreen and never touch the desktop.
- [site] `peekaboo list windows --app Minne` binds to *one* pid, so it cannot see a second instance's windows. A 12-line `swift` script over `CGWindowListCopyWindowInfo` filtered by owner pid gives id/bounds/alpha per process (kept at `site/`-adjacent scratch during the build); `screencapture -x -o -l <id>` then captures that window with real alpha and rounded corners.
- [site] Window captures of Minne panels composite with transparency, so `magick <shot> -fuzz 12% -trim +repage` yields a panel that drops onto any background. Quantising UI screenshots (`-strip -colors 256`) shrinks them 3–4× losslessly to the eye — except on the overlay's *dark* placeholder gradients, where it leaves a visible moiré once the browser downscales it. Leave dark gradients unquantised.
- [site] `bun build --minify` of a three.js scene is ~490 KB no matter how few symbols you import — `three` ships a wildcard `sideEffects` field bun cannot use, so the renderer never tree-shakes away. Budget for it, load it as a `type=module` script (deferred by default) and fade the canvas in.
- [site] Scroll reveals must be gated on a class the page's own script adds (`<html class="js">`), or a JS failure leaves every section at `opacity: 0` — and a full-page screenshot shows the same thing, since IntersectionObserver never fires for content outside the viewport.
- [site] The site is plain static files: `cd site && python3 -m http.server 8765`. Only `assets/hero.js` and `assets/hero-static.svg` are generated (`bun run build`, `bun run build:svg`), and both are committed so nothing has to be built to deploy. See `site/README.md`.
- [site] **The app's surfaces on the site are DOM replicas, not screenshots** (`.key`, `.chat`, `.pane` in styles.css, timelines in `assets/ui.js`): screenshots went stale the moment the UI moved, and a replica can act — the key overlay thinks, streams a draft and offers Insert; the chat searches, then streams the answer. They mimic the app rather than clone it, so when the app changes, hand-edit them. Every timeline is paused by an IntersectionObserver off screen and holds a finished state under `prefers-reduced-motion`.
- [site] Chrome caches `styles.css` aggressively across headless reloads — a CSS change can look like it did nothing for several iterations (`setExtraHTTPHeaders({'Cache-Control':'no-cache'})` and CDP `Network.setCacheDisabled` both failed to help). What works: after `page.goto`, re-point the link (`l.href = l.href.split('?')[0] + '?b=' + Date.now()`) and wait a beat. Check `document.styleSheets[0].cssRules.length` before believing a screenshot.
- [site] three.js uniform arrays (`uniform vec3 uWave[2]`) take a flat `Float32Array` as their `value` — no need for an array of `Vector3`. That is how the hero runs two digest waves at once.
- [site] The hero's assembly (fragments flying in and snapping into the graph) re-runs whenever the hero re-enters the viewport after 4 s away, by rebasing the `aDelay`/`aFormed` attributes on the current clock. Without that the one real "moment" of the page only ever plays in the first four seconds of a visit — which is exactly the feedback the first version got.
- [site] **The page is now the demo** (`#demo` in index.html, `assets/demo.js`): one stage plays the same story — caret, right-Option, Minne thinks, the draft streams, Insert lands it — across four scenes. A scene is markup carrying its *finished* text; the script copies that text, empties the field and plays it back, which is why the page still reads with JavaScript off or motion reduced. Adding a moment = one `.scene` + one `.tab` + one line in `SAYS`. Three.js was cut in the same pass: two spectacles competed, and the demo is the one that sells the product (it also took 490 KB off the page).
- [site] Two traps when a class both *shows* an element and *marks a state*: `className.replace(/ is-[\w-]+/g, "")` to clear per-scene states also eats `is-on`, which is what makes the scene visible — the window vanished and only the overlay was left. Keep an explicit list of transient state classes. And a state class left over from the previous cycle (`has-text`) lit the Send button before anything had been written; reset every state on entry, not just the ones the phase sets.
- [site] `page.addInitScript` in a Playwright MCP session sticks to that page for **every later navigation**: an init script added to fake a no-JS visit silently kept stripping the `js` class for the rest of the session, and the tabs stayed hidden while I hunted a CSS bug that did not exist. Close the page (`browser_close`) after any init-script test.
- [polish] The grey the overlay was full of was mostly *inherited*, not painted: `NSVisualEffectView(material: .popover)` averages whatever is behind the panel, and a stock quiet `NSButton` fill (`labelColor` @ 0.09) plus the draft's card (`textBackgroundColor` @ 0.5 + `separatorColor` border) added the rest. A committed surface colour is the only way a panel is actually white — vibrancy cannot be told to be.
- [polish] `controlAccentColor` is the *user's* accent, so a product whose identity is a colour must not use it: on a graphite or pink Mac the spark, the dots and the primary capsule all stop being blue. `OverlayPalette` fixes one blue instead (light #0A5CDE, dark #4C8DFF for glyphs / #3373EB for the fill, which needs to be deeper so white text on it clears 4:1).
- [polish] `NSColor(name: nil, dynamicProvider:)` + `appearance.bestMatch(from: [.aqua, .darkAqua])` gives dynamic colours that keep the existing `viewDidChangeEffectiveAppearance` → re-read `.cgColor` pattern working, and let `textColor`/`contentTintColor` follow the appearance with no code at all. One catch: `blended(withFraction:of:)` returns nil on a dynamic colour — `usingColorSpace(.sRGB)` first.
- [polish] Alignment, measured: shapes (the rule, the capsules) sit at the layout inset exactly; text ink lands 0.5–1 pt inside it because glyphs carry side bearing. That 1 pt is correct and invisible. What the user actually saw was the draft card's own 11 pt padding, which put the draft's left edge 11 pt right of every other child. Deleting the card *is* the alignment fix.
- [polish] `measure.py` in the session scratchpad reads a window capture back and prints, per band of ink, its top/bottom in points and its distance from each border — which is how "equal padding top and bottom" and "one inset" were checked rather than eyeballed. A window capture is exactly the window's bounds at 2×, so pixels ÷ 2 = points.
- [polish] Screenshotting the overlay while the user's own Minne runs: launch the *bare SwiftPM binary* (never `scripts/build.sh`, which `rm -rf`s `build/Minne.app` out from under the running app) with `-simulateNoAccessibility YES -minneKeyEnabled NO -chatHotKeyEnabled NO -onboardingSeen YES` plus scratch `MINNE_APP_SUPPORT_DIR`/`MINNE_MEMORY_ROOT`, then poll `CGWindowList` for the panel's id and kill by pid the moment the capture is taken (~0.5 s on screen per state). `scratchpad/shot.sh`.
- [polish] `NSStackView.setCustomSpacing(_:after:)` survives `setViews(_:in:)` being called once at build time and is how a column gets a rhythm (9 / 11 / 13) instead of one uniform gap; hidden arranged subviews collapse their custom spacing with them, so a state that hides the draft still spaces correctly.
- [polish] Styling a *suffix* of a string must be gated on the caller's knowledge, not on the string's shape: matching "\n\n[…]" to dim the elision note also dims a bracketed line the user's own draft ends with. `body(_:elided:)` takes the flag; the test that caught it is `testAnUnelidedDraftIsAllOneInk`.
- [guide] A `.nonactivatingPanel` **can** hold key status without activating the app — that is how the overlay grew a text field. Make `canBecomeKey` a variable that is true only while the field is being edited (plus `becomesKeyOnlyIfNeeded = true`), `makeKeyAndOrderFront`, and hand key back with `orderOut(nil)` + `orderFrontRegardless()`: AppKit has no "resign key" call, and the window server then gives it to the frontmost app's own window.
- [guide] `becomesKeyOnlyIfNeeded` means the panel takes key only when something in it *already* needs the keyboard — so make the field first responder **before** `makeKeyAndOrderFront`, not after. The other order silently fails: the panel never becomes key and the field draws unfocused with no caret (verified live, first attempt).
- [guide] AppKit makes the guidance field the panel's **initial first responder — field editor and all — the moment the overlay is ordered front**, so "is the field first responder" is not "does the field have the keyboard". The test has to be `window.isKeyWindow` *and* first responder; without the first half the focus ring lights on every draft and, far worse, the event tap stops claiming Escape and Return because the controller thinks the user is typing a steer.
- [guide] Verified live (AX probe before / during / after, against Ghostty and against Chrome): while the panel holds key, the target app stays **frontmost** and its `AXFocusedUIElement` and selection are unchanged, and it gets the keyboard back on `endGuiding()`. `NSApp.isActive` does go true inside Minne — which is why the app-switch dismissal has to ignore Minne itself — but the workspace's frontmost app never changes. Recipe: `-minneKeyPreview guiding -minneKeyPreviewGuiding key` on an idle machine (`ioreg -c IOHIDSystem | grep HIDIdleTime`), never while somebody is typing. While the guidance field is being edited the controller must therefore claim **nothing** — Return, Escape, ⌘Z, ⌘R and Tab all pass — or the field never receives what the user is typing into it. `MinneKeyController.command` checks `presenter.isGuiding` before anything else.
- [guide] Guard the app-switch dismissal against Minne itself (compare `NSWorkspace.applicationUserInfoKey`'s pid with `ProcessInfo.processIdentifier`). Without it, if borrowing key ever *did* activate the app, the overlay would dismiss itself the instant it was typed into.
- [guide] A synthetic ⌘V goes to whoever holds the keyboard, so an insertion that follows a guidance round has to give key back **and wait** (`MinneKeyController.focusReturnDelay`, 0.12 s) — the hand-off crosses two processes and is not done when `endGuiding()` returns.
- [guide] `NSStackView` packs a horizontal row at its leading edge the moment the stretchy view stops stretching — an `isEditable = false` NSTextField did exactly that and the ⇥ hint jumped from the right edge to the middle of the field. A trailing accessory wants explicit constraints, not a stack.
- [guide] An SF Symbol inside an `attributedTitle` is drawn **black** whatever the string's foreground colour says: tint it by hand (`lockFocus` + `fill(using: .sourceAtop)`) and rebuild the title inside `effectiveAppearance.performAsCurrentDrawingAppearance`, because that tint is pixels and not a dynamic colour. Stroked glyphs also need a heavier weight than the label beside them to read as the same weight.
- [guide] Reworking a draft shimmers the *previous draft* — alpha 0.5 plus a gradient-mask sweep on the label's own layer — rather than swapping in the placeholder bars. The panel keeps its height, the user keeps their place, and the state reads as "this text, being rewritten".
- [guide] The `draft` request carries `previousDraft` + `guidance[]` + `regenerate` because every draft is a **fresh Agent with no transcript**: without them the model has no idea it is being asked a second time and writes the same sentences back. Regenerate says "differ from this", guidance says "revise this, keep what I did not object to"; steers accumulate and both are cited alongside the style page.
- [guide] zsh's builtin `echo` interprets `\n`, which silently splits one JSON-lines request into two invalid lines when driving the brain from a shell script (the brain answers `invalid_json` with an empty id). Use `printf`, or keep escapes out of the fixture.
